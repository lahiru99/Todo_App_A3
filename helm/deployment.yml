apiVersion: apps/v1
kind: Deployment
metadata:
  name: "todo-app"

spec:
  selector:
    matchLabels:
      app: "todo-app"
  replicas: 4
  template:
    metadata:
      labels:
        app: "todo-app"
    spec:
      containers:
      - image: ${IMAGE_TAG}
        imagePullPolicy: Always
        name: "todo-app"
        ports:
         - containerPort: 80  